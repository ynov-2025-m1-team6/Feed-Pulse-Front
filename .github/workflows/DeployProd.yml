name : Deploy to Staging

on:
  push:
    branches:
        - main

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
        - name: Deploy Docker
          run : curl ${{secrets.RENDER_DEPLOY_HOOK}}
